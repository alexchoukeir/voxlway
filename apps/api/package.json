{
    "name": "api",
    "version": "0.0.0",
    "scripts": {
        "dev": "uv run fastapi dev main.py --port 8000",
        "lint": "uv run ruff check . --no-cache",
        "test": "uv run pytest tests/ -v"
    }
}
